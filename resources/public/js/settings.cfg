
	BASE_WIDTH = 800;
	BASE_HEIGHT = 350;

//GRAPHICS
	FPS_RATE = 60;
//WORLD
	MOVEMENT_SPEED = 0.01 * FPS_RATE;
	MAX_MOVEMENT_SPEED = MOVEMENT_SPEED + 0.04;
// NETWORK
	// Increase for smoother updates but lowers performance
	UPDATE_RATE = 0.01;

	// New Map Interval
	MESSAGE_INTERVAL = 0.50;

	NEW_AREA = 0;
	NEW_AREA_WIDTH  = (BASE_WIDTH / 2) + NEW_AREA - 250;
	NEW_AREA_HEIGHT = (BASE_HEIGHT / 2) + NEW_AREA;

	ENABLE_SHADOWS = false;

