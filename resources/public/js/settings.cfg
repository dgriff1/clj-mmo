
	BASE_WIDTH = 800;
	BASE_HEIGHT = 350;

//GRAPHICS
	FPS_RATE = 60;
//WORLD
	MOVEMENT_SPEED = 1.5;
	MAX_MOVEMENT_SPEED = 3.0;
// NETWORK
	// Increase for smoother updates but lowers performance
	UPDATE_RATE = 0.03;
	MESSAGE_INTERVAL = 0.60;

	NEW_AREA = 1000;
	NEW_AREA_WIDTH  = (BASE_WIDTH / 2) + NEW_AREA - 200;
	NEW_AREA_HEIGHT = (BASE_HEIGHT / 2) + NEW_AREA;

